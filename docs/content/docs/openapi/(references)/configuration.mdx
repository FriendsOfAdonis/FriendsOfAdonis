---
title: Configuration
description: 'Configure the OpenAPI package for AdonisJS. Customize the UI, document metadata, type loaders, and controller discovery.'
---

```ts title="config/openapi.ts" twoslash
// @noErrors
import { defineConfig } from '@foadonis/openapi'

export default defineConfig({
  ui: 'scalar',

  document: {
    info: {
      title: 'My API',
      version: '1.0.0',
    },
  },

  loaders: [MyCustomLoader],

  controllers: [CustomController],
})
```
