---
title: Configuration
description: "Reference for config/openapi.ts in @foadonis/openapi: the ui renderer, document info metadata, custom type loaders and extra controller registration."
---

```ts title="config/openapi.ts" twoslash
// @noErrors
import { defineConfig } from '@foadonis/openapi'

export default defineConfig({
  ui: 'scalar',

  document: {
    info: {
      title: 'My API',
      version: '1.0.0',
    },
  },

  loaders: [MyCustomLoader],

  controllers: [CustomController],
})
```
